"""Direct graph CLI is retired. Use the web UI."""


def main() -> None:
    print(
        "The retrieval-loop CLI is retired. Run the web UI:\n"
        "  uv run self-healing-rag-server\n"
        "  cd web && npm run dev"
    )
