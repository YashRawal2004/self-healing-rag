"""Folder ingest is retired. Upload PDFs in a chat from the web UI."""


def main() -> None:
    print(
        "Folder ingest is gone. Sign in, open a chat, and upload PDFs there.\n"
        "Chunks live in Neon, scoped to that chat."
    )
